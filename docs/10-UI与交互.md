# 10 —— UI 与交互

> V1 极简。先做功能，上线后再做立绘。

---

## 叙事 UI

- V1：**纯对话框**（底部文字框）
- 上线后可补充：对话框上方显示角色立绘

---

## HUD

> ⏳ 待 MVP 阶段随功能逐步定义。预计包含：
- 血条/魔能条
- 经验条
- 技能图标 ×3 + 冷却显示
- 宠物头像 + 血条
- 小地图（可选）
- 金币数量
- 当前装备耐久度提示

---

## 菜单系统

> ⏳ 待 MVP 阶段定义。预计包含：
- 背包/物品栏
- 角色属性面板（显示转职门槛）
- 宠物列表
- 副职业面板
- 地图
- 设置

---

## 对话保护

- 对话期间角色进入**过场状态**（无敌 + 不可移动）
- 避免在刷怪区放对话 NPC

---

## 奖励发放

- 任务奖励无视背包容量
- 普通奖励满包时发到**临时邮箱/暂存箱**（24 小时限领）
- 重要物品直接越过容量检查发放

---

## 邮件系统 UI（MVP 简化版）

| 功能 | MVP | 联机 |
|------|:--:|:--:|
| 邮件列表显示 | ✅ | ✅ |
| 邮件附件领取 | ✅ | ✅ |
| 邮件过期 24h 倒计时显示 | ✅ | ✅ |
| 红点提示未读邮件 | ✅ | ✅ |
| 玩家间邮件 UI | ❌ | ⏳ |
| GM 群发 UI | ❌ | ✅ |

### MVP 邮件流程

```
玩家满包时获得奖励 → 自动入邮件 → 邮箱红点提示
→ 玩家打开邮箱 → 看到邮件 + 附件 → 点领取 → 附件入包 → 邮件标记已读
→ 24h 后邮件过期删除（附件未领则提示丢失）
```

数据配置走 `data/mails.json`，逻辑走 `scripts/core/mail_manager.gd`。

---

## 伤害飘字（Phase 4.4.6 步骤 1 落地）

### 设计目标

战斗中所有受击事件（玩家/宠物/怪物）都弹出伤害数值，让玩家直观看到战斗情况。

### 实现方式

- **Autoload 单例**：`DamageNumberManager`（注册在 `project.godot` 的 `[autoload]` 段）
- **场景**：`scenes/ui/damage_number.tscn`（Label 节点）
- **脚本**：`scripts/ui/damage_number.gd` + `scripts/ui/damage_number_manager.gd`
- **调用入口**：`DamageNumberManager.show_damage_number(world_pos, amount, type)`

### 飘字类型

| 类型 | 颜色 | 文本 | 触发场景 |
|------|------|------|---------|
| `damage` | 红色 `(1, 0.3, 0.3)` | 数值 | 受伤（玩家/宠物/怪物）|
| `miss` | 灰色 `(0.7, 0.7, 0.7)` | "miss" | 攻击落空（步骤 2 实现）|
| `heal` | 绿色 `(0.3, 1, 0.3)` | "+数值" | 治疗（未来加）|

### 动画

- 浮动：向上 40 像素（`FLOAT_DISTANCE = 40.0`）
- 淡出：0.8 秒（`FADE_DURATION = 0.8`）
- Tween 并行执行（`set_parallel(true)`），完成后 `queue_free()`

### 触发点

| 单位 | 触发位置 | 说明 |
|------|---------|------|
| 玩家攻击怪物命中 | `player.gd._check_hit()` → `_spawn_damage_number()` | 玩家看到自己打多少 |
| 玩家受伤 | `player.gd.take_damage()` → `_spawn_damage_number()` | 玩家看到自己挨多少 |
| 宠物攻击怪物命中 | `pet.gd._check_pet_hit()` → `DamageNumberManager.show_damage_number()` | 玩家看到宠物打多少 |
| 宠物受伤 | `pet.gd.take_damage()` → `DamageNumberManager.show_damage_number()` | 玩家看到宠物挨多少 |
| 怪物攻击命中 | `monster.gd._do_attack()` → `DamageNumberManager.show_damage_number()` | 玩家看到怪物打多少 |
| 怪物受伤 | `monster.gd.take_damage()` → `DamageNumberManager.show_damage_number()` | 玩家看到怪物挨多少 |

### 位置规则

- 飘字位置 = 受击单位 `global_position + Vector2(0, -20)`
- 向上偏移 20 像素避免覆盖单位本身
- 飘字作为 Label 节点加入 `get_tree().current_scene`，使用世界坐标，跟随场景滚动

### 数据驱动

伤害数值来自 `take_damage(amount)` 的 `amount` 参数，不需要新 JSON 文件（§1.4 数据文件清单不新增）。

### 未来扩展

- 字体大小/颜色可配置化（数据驱动）
- 暴击伤害用黄色大字体
- 多段伤害连续弹出（错开位置）
- 伤害飘字池化优化（避免频繁实例化）
